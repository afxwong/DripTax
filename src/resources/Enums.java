package resources;

public class Enums {
    public enum Difficulty {
        Hard,
        Medium,
        Easy
    }

    public enum Element {
        Fire,
        Water,
        Ground,
        Air
    }
}

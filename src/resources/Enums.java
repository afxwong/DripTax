package resources;

public class Enums {

    public enum Difficulty {
        Hard,
        Medium,
        Easy
    }

    public enum Element {
        Fire,
        Water,
        Ground,
        Air,
        Boss
    }

    public enum EnemyLane {
        Top,
        Bottom
    }

    public enum TowerLane {
        Top,
        Middle,
        Bottom
    }
}
